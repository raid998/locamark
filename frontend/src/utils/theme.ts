import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ffa726",
      light: "#ffb851",
      dark: "#b2741a",
    },
    secondary: { main: "#c62828", light: "#d15353", dark: "#8a1c1c" },
  },
});
export { theme };
